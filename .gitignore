node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Los datos del negocio NO se versionan: pedidos con costos, precios y márgenes,
# el catálogo y las fotos. Quedan solo en el disco, con OneDrive como respaldo.
# Este repo es publicable porque acá no hay nada del negocio.
datos/
